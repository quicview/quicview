[package]
name = "bridge"
version = "0.1.0"
edition = "2021"
license = "Apache-2.0"
description = "Abstractions to interface QuicView config with upstream RustDesk components"

[features]
# future: enable when wiring directly to rustdesk crates/binaries
rustdesk = []

[dependencies]
thiserror = "1.0"
config = { path = "../config" }
