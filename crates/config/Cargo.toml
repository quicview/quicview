[package]
name = "config"
version = "0.1.0"
edition = "2021"
license = "Apache-2.0"
description = "Config models and loading for QuicView (reusing RustDesk upstream)"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_yaml = "0.9"
thiserror = "1.0"
